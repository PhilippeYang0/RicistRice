-- Translated by hand from workspaces.conf, in the format nwg-displays writes
-- (it overwrites this file whenever you apply workspaces in its GUI).

hl.workspace_rule({
    workspace = "1",
    monitor = "DP-1",
    default = true
})
hl.workspace_rule({
    workspace = "2",
    monitor = "DP-2",
    default = true
})
